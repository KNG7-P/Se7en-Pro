<UserControl x:Class="PsiphonUI.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:PsiphonUI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LogsViewModel}">

<DockPanel Margin="32,24,32,32" LastChildFill="True">

        <StackPanel DockPanel.Dock="Top">
            <TextBlock Style="{StaticResource UI.Title}" Text="{Binding Title}" />
            <TextBlock Style="{StaticResource UI.Subtitle}" Text="Live notices from the tunnel core." />
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Filter..."
                     materialDesign:TextFieldAssist.HasLeadingIcon="True"
                     materialDesign:TextFieldAssist.LeadingIcon="Magnify" />
            <Button Grid.Column="1" Content="Copy"
                    Command="{Binding CopyCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="8,0,0,0" />
            <Button Grid.Column="2" Content="Clear"
                    Command="{Binding ClearCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="8,0,0,0" />
        </Grid>

<Border Style="{StaticResource UI.Card}" Padding="12" Margin="0,0,0,0">
            <ListBox ItemsSource="{Binding FilteredLines}"
                     FontFamily="Cascadia Code, Consolas, Courier New, monospace"
                     FontSize="12"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     VirtualizingPanel.CacheLength="2,2"
                     VirtualizingPanel.CacheLengthUnit="Page">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">

                        <Setter Property="Padding" Value="2,1" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>
    </DockPanel>
</UserControl>
